import * as React from 'react';

const AddButton = () => {

    return (

        <div className='container my-3 p-0'>
            <button className='btn rounded border w-100' style={{ color: 'black' }}>Agregar al Carrito</button>
        </div>

    )
}

export default AddButton;